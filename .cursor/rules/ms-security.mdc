---
description: Convenciones Spring Boot para ms-security (DTOs, secretos, API pública)
globs: ms-security/**/*.java
alwaysApply: false
---

# ms-security

- Exponer DTOs en controllers; entidades JPA (`models/`) solo en services/repositories.
- Secretos en `ms-security/.env` (ver `.env.example`); no commitear `.env`.
- Rutas públicas bajo `/api/public/`; RBAC admin en `/api/roles`, `/api/permissions`, etc.
- Cambios en `validate-token` afectan ms-business: coordinar contrato y roles.
- Passwords: **BCrypt** (`EncryptionService` / `BCryptPasswordEncoder`).
- Verificación rápida: `.agents/skills/ms-security/scripts/build.sh` (solo compile). CI: `./mvnw verify`. Skill: `.agents/skills/ms-security/SKILL.md`.
