export enum IncidentType {
  MECHANICAL = 'mechanical',
  ACCIDENT = 'accident',
  DELAY = 'delay',
  PASSENGER = 'passenger',
  OTHER = 'other',
}

export enum IncidentSeverity {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}

export enum IncidentStatus {
  REPORTED = 'reported',
  IN_REVIEW = 'in_review',
  CLOSED = 'closed',
}
